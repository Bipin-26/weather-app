import { db } from "./firebase.utils"
import { v4 as uuidv4 } from 'uuid'

const Firebase={
    readDoc:(...args) => {

    },
    writeDoc:(...args) => {
        
    },
    deleteDoc:(...args) => {

    }
}